[build]
  command = "npm run build"
  publish = "dist"

# Public backend origin. File configuration overrides stale Netlify UI values.
[build.environment]
  VITE_API_BASE_URL = "https://tgdgbackend-production.up.railway.app"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
